//build a setinterval that runs every 1000ms
let Time = setInterval(1)
//build a global variables that starts at zero
var score = 0
//when the js interval reaces 1 ms
if (Time = 0){
    //add one to the global variable
    score += 1
}